import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    modalContainer: {
        position: 'absolute',
        top: 60,
       height: 200,
       width: 340,
       paddingTop: 170,
       marginTop: 60,
     }
});
