import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    modalContainer: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
      // dieu chinh kich thuoc, backgroundColor modal o day khong co y nghia ne se khong an
      // width:100,
      // height:700,
      // backgroundColor:'#ffff',
     }

});
