import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    width: 340,
    position: 'relative'
  },
  modalContainer: {
     position: 'absolute',
     top: 60,
    height: 200,
    width: 340,
    paddingTop: 170,
    marginTop: 60,
  }
});
